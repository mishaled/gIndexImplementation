import { Graph, GraphDatabase } from '../Model';

export class FeatureSelector {
    constructor() {

    }

    public Select(graphDatabase: GraphDatabase, discriminativeRatio: number, sizeIncreasingSupportFunction: Function, maxFragmentSize: number): Graph[] {
        throw 'Not implemented yet';
    }
}